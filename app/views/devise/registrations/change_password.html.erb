<div class="border-form-div">
	<p class="form-top-message">Change your password</p>
	<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, novalidate: :novalidate }, data: {validate: 'parsley'}) do |f| %>
	  
	  <div><%= f.label :current_password, "Current password", :class => 'ticker' %><br />
	    <%= f.password_field :current_password, autofocus: true, autocomplete: "off", required: true %></div>

	  <div><%= f.label :password, "New password", :class => 'ticker' %><br />
	    <%= f.password_field :password, autocomplete: "off", required: true %></div>

	  <div><%= f.label :password_confirmation, "Confirm new password", :class => 'ticker' %><br />
	    <%= f.password_field :password_confirmation, autocomplete: "off", required: true  %></div>

	  <%= button_tag(type: 'submit', class: "ladda-button", 'data-style' => "zoom-out") do %>
			<span class="ladda-label" style="font-size: 20px">Change Password</span>
	  <% end %>
	<% end %>

</div>

<%=javascript_include_tag "ladda.min.js", "spin.min.js" %>

<script>
$( document ).ready(function() { 
	Ladda.bind( '.ladda-button' );
	$(".ladda-button").click(function() { 
		if ($("[name='user[password]']").val() != $("[name='user[password_confirmation]']").val()) { 
			Ladda.stopAll();
		} 
	});
	
}); 
</script>